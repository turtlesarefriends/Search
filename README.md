# GitHub Pages version

Upload `index.html` and `data.js` to a GitHub repository and enable GitHub Pages.

This is the clickable static prototype. The interface supports all 50 states, but the bundled records are demo records. A production data refresh should generate `data.js` from current OneMain branch data and current official state legislative district data. Do not expose API keys in GitHub Pages JavaScript.
